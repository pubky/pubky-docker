services:
  # 1. PostgreSQL (shared by Homeserver and Homegate)
  postgres:
    profiles: [backend]
    container_name: postgres
    image: postgres:17-alpine
    restart: always
    networks:
      pubky:
        ipv4_address: "172.18.0.9"
    volumes:
      - .storage/postgres/data:/var/lib/postgresql/data
    env_file:
      - ./.env
    ports:
      - 5432:5432
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5

  # 2. Pubky Homeserver
  homeserver:
    profiles: [backend]
    container_name: homeserver
    image: "${REGISTRY:-synonymsoft}/homeserver-${HOMESERVER_ENV:-testnet}:${HOMESERVER_TAG:-latest}"
    restart: always
    build:
      context: ../pubky-core
    volumes:
      - ./homeserver.config.toml:/config.toml
      - ./homeserver.entrypoint.sh:/entrypoint.sh
    env_file:
      - ./.env
    entrypoint: [ "/bin/sh", "-c", "/entrypoint.sh" ]
    networks:
      pubky:
        ipv4_address: "172.18.0.4"
    ports:
      - 6287:6287
      - 6286:6286
      - 6288:6288
      - 15411:15411
      - 15412:15412
    depends_on:
      postgres:
        condition: service_healthy

  # 3. Nexus
  # 3.1 Pubky Nexus Daemon
  nexusd:
    profiles: [backend]
    container_name: nexusd
    image: "${REGISTRY:-synonymsoft}/pubky-nexus:${PUBKY_NEXUS_TAG:-latest}"
    build:
      context: ../pubky-nexus
    # Simplified internal config path
    command: nexusd --config-dir=/config
    volumes:
      # Mount static files path
      - .storage/static:/static
      # Mount the config file
      - ./pubky-nexus-config-${NETWORK:-testnet}.toml:/config/config.toml
    ports:
      - 8080:8080
      - 8081:8081
    networks:
      pubky:
        ipv4_address: "172.18.0.3"
    depends_on:
      - nexus-neo4j
      - nexus-redis

  # 3.2 Pubky Nexus Graph
  nexus-neo4j:
    profiles: [backend]
    image: neo4j:5.26.7-community
    container_name: nexus-neo4j
    restart: unless-stopped
    ports:
      - 7474:7474
      - 7687:7687
    networks:
      pubky:
        ipv4_address: "172.18.0.5"
    volumes:
      # Mount the neo4j configuration file to container
      - .storage/neo4j/conf:/conf
      # Mount the data to container
      - .storage/neo4j/data:/data
      - .storage/neo4j/logs:/logs
      - ./pubky-nexus/docker/db-graph:/db-graph
    env_file:
      - ./neo4j.env

  # 3.3 Pubky Nexus Index
  nexus-redis:
    profiles: [backend]
    image: redis:8.0.6-alpine
    container_name: nexus-redis
    ports:
      - 6379:6379
    networks:
      pubky:
        ipv4_address: "172.18.0.6"
    volumes:
      - .storage/redis/data:/data
      - ./redis.conf:/usr/local/etc/redis/redis.conf
    command: redis-server /usr/local/etc/redis/redis.conf
    restart: always

  nexus-redisinsight:
    profiles: [backend]
    image: redis/redisinsight:3.0.3
    container_name: nexus-redisinsight
    ports:
      - 5540:5540
    networks:
      pubky:
        ipv4_address: "172.18.0.7"
    environment:
      - RI_REDIS_HOST=nexus-redis
      - RI_REDIS_PORT=6379
    depends_on:
      - nexus-redis
    restart: always

  # 4. Pubky App Social UI
  pubky-app:
    profiles: [pubky-app]
    container_name: pubky-app
    image: "${REGISTRY:-synonymsoft}/pubky-app:${PUBKY_APP_TAG:-latest}"
    build:
      context: ../pubky-app
      args:
        NEXT_PUBLIC_DB_VERSION: ${NEXT_PUBLIC_DB_VERSION:-2}
        NEXT_PUBLIC_DB_NAME: ${NEXT_PUBLIC_DB_NAME:-franky}
        NEXT_PUBLIC_DEBUG_MODE: ${NEXT_PUBLIC_DEBUG_MODE:-false}
    env_file:
      - ./.env
    environment:
        PUBKY_RUNTIME_SYNC_TTL: ${NEXT_PUBLIC_SYNC_TTL:-300000}
        PUBKY_RUNTIME_NOTIFICATION_POLL_INTERVAL_MS: ${NEXT_PUBLIC_NOTIFICATION_POLL_INTERVAL_MS:-8888}
        PUBKY_RUNTIME_NOTIFICATION_POLL_ON_START: ${NEXT_PUBLIC_NOTIFICATION_POLL_ON_START:-false}
        PUBKY_RUNTIME_NOTIFICATION_RESPECT_PAGE_VISIBILITY: ${NEXT_PUBLIC_NOTIFICATION_RESPECT_PAGE_VISIBILITY:-true}
        PUBKY_RUNTIME_STREAM_POLL_INTERVAL_MS: ${NEXT_PUBLIC_STREAM_POLL_INTERVAL_MS:-8888}
        PUBKY_RUNTIME_STREAM_POLL_ON_START: ${NEXT_PUBLIC_STREAM_POLL_ON_START:-false}
        PUBKY_RUNTIME_STREAM_RESPECT_PAGE_VISIBILITY: ${NEXT_PUBLIC_STREAM_RESPECT_PAGE_VISIBILITY:-true}
        PUBKY_RUNTIME_STREAM_FETCH_LIMIT: ${NEXT_PUBLIC_STREAM_FETCH_LIMIT:-10}
        PUBKY_RUNTIME_STREAM_CACHE_MAX_AGE_MS: ${NEXT_PUBLIC_STREAM_CACHE_MAX_AGE_MS:-300000}
        PUBKY_RUNTIME_TTL_POST_MS: ${NEXT_PUBLIC_TTL_POST_MS:-300000}
        PUBKY_RUNTIME_TTL_USER_MS: ${NEXT_PUBLIC_TTL_USER_MS:-600000}
        PUBKY_RUNTIME_TTL_BATCH_INTERVAL_MS: ${NEXT_PUBLIC_TTL_BATCH_INTERVAL_MS:-5000}
        PUBKY_RUNTIME_TTL_POST_MAX_BATCH_SIZE: ${NEXT_PUBLIC_TTL_POST_MAX_BATCH_SIZE:-20}
        PUBKY_RUNTIME_TTL_USER_MAX_BATCH_SIZE: ${NEXT_PUBLIC_TTL_USER_MAX_BATCH_SIZE:-20}
        PUBKY_RUNTIME_TTL_RETRY_DELAY_MS: ${NEXT_PUBLIC_TTL_RETRY_DELAY_MS:-60000}
        PUBKY_RUNTIME_HOMESERVER: ${NEXT_PUBLIC_HOMESERVER:-8pinxxgqs41n4aididenw5apqp1urfmzdztr8jt4abrkdn435ewo}
        PUBKY_RUNTIME_HOMESERVER_URL: ${NEXT_PUBLIC_HOMESERVER_URL:-http://localhost:6286}
        PUBKY_RUNTIME_NEXUS_URL: ${NEXT_PUBLIC_NEXUS_URL:-http://localhost:8080}
        PUBKY_RUNTIME_CDN_URL: ${NEXT_PUBLIC_CDN_URL:-http://localhost:8080/static}
        PUBKY_RUNTIME_TESTNET: ${NEXT_PUBLIC_TESTNET:-true}
        PUBKY_RUNTIME_PKARR_RELAYS: ${NEXT_PUBLIC_PKARR_RELAYS:-["https://pkarr.pubky.app","https://pkarr.pubky.org"]}
        PUBKY_RUNTIME_DEFAULT_HTTP_RELAY: ${NEXT_PUBLIC_DEFAULT_HTTP_RELAY:-http://localhost:15412/inbox/}
        PUBKY_RUNTIME_EXCHANGE_RATE_API: ${NEXT_PUBLIC_EXCHANGE_RATE_API:-https://api1.blocktank.to/api/fx/rates/btc}
        PUBKY_RUNTIME_HOMEGATE_URL: ${NEXT_PUBLIC_HOMEGATE_URL:-http://localhost:6300}
        PUBKY_RUNTIME_MODERATION_ID: ${NEXT_PUBLIC_MODERATION_ID:-euwmq57zefw5ynnkhh37b3gcmhs7g3cptdbw1doaxj1pbmzp3wro}
        PUBKY_RUNTIME_MODERATED_TAGS: ${NEXT_PUBLIC_MODERATED_TAGS:-["nudity"]}
        NEXT_ENABLE_PLAUSIBLE: ${NEXT_ENABLE_PLAUSIBLE:-false}
        NEXT_MAX_STREAM_TAGS: ${NEXT_MAX_STREAM_TAGS:-5}
    networks:
      pubky:
        ipv4_address: "172.18.0.8"
    ports:
      - 3000:3000

  # 5. Homegate
  # 5.1 PostgreSQL init for Homegate
  homegate-db-init:
    profiles: [backend]
    container_name: homegate-db-init
    image: postgres:17-alpine
    env_file:
      - ./.env
    entrypoint:
      - /bin/sh
      - -c
      - |
        export PGPASSWORD="$POSTGRES_PASSWORD"
        until pg_isready -h postgres -U "$POSTGRES_USER" -d postgres; do
          sleep 1
        done
        if ! psql -h postgres -U "$POSTGRES_USER" -d postgres -Atc "SELECT 1 FROM pg_database WHERE datname = 'pubky_homegate'" | grep -qx 1; then
          psql -h postgres -U "$POSTGRES_USER" -d postgres -c "CREATE DATABASE pubky_homegate"
        fi
    restart: "no"
    networks:
      pubky:
        ipv4_address: "172.18.0.11"
    depends_on:
      postgres:
        condition: service_healthy

  # 5.2 Fake Prelude API for local SMS verification
  homegate-prelude:
    profiles: [backend]
    container_name: homegate-prelude
    image: wiremock/wiremock:latest
    command:
      - --global-response-templating
    volumes:
      - ./homegate-prelude:/home/wiremock
    networks:
      pubky:
        ipv4_address: "172.18.0.12"

  # 5.3 Homegate
  homegate:
    profiles: [backend]
    container_name: homegate
    image: "${REGISTRY:-synonymsoft}/homegate:${HOMEGATE_TAG:-latest}"
    restart: always
    build:
      context: ../homegate
    volumes:
      - ./.storage/homegate:/data
      - ./homegate.config.toml:/config.toml:ro
      - ./homegate.entrypoint.sh:/entrypoint.sh:ro
    entrypoint: [ "/bin/sh", "-c", "/entrypoint.sh" ]
    networks:
      pubky:
        ipv4_address: "172.18.0.10"
    ports:
      - 6300:6300
    depends_on:
      homeserver:
        condition: service_started
      homegate-db-init:
        condition: service_completed_successfully
      homegate-prelude:
        condition: service_started

networks:
  pubky:
    driver: bridge
    enable_ipv6: false
    ipam:
      driver: default
      config:
        - subnet: 172.18.0.0/16

volumes:
  backend_storage:
